<% include tiles/header %>
<% include tiles/top %>

<div class="container">
    <div class="row">
      
      <div id="sidebar" class="col-md-4">
          <% include tiles/sidebar %>
      </div>
      <div class="col-md-5">
          <h3 class="my-4">Rejestracja</h3>

          <% if (form.add) { %>
            Zarejestrowano. <a href="/login">Zaloguj się</a>
          <% } else { %>

            <% if(form.errorMsg !== "") { %>
              <div class="alert alert-danger">
                  <%= form.errorMsg %><br />
              </div>
            <% } %>
          <form action="" method="post">
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" name="email" class="form-control" id="email" placeholder="Email" value="<%= form.email %>">
            </div>
            <div class="form-group">
              <label for="username">Nazwa</label>
              <input type="text" name="username" class="form-control" id="username" placeholder="Nazwa" value="<%= form.username %>">
          </div>
            <div class="form-group">
                <label for="password">Hasło</label>
                <input type="password" name="password" class="form-control" id="password" placeholder="Hasło">
            </div>
            <div class="form-group">
              <label for="password2">Powtórz hasło</label>
              <input type="password" name="password2" class="form-control" id="password2" placeholder="Hasło">
          </div>
            <button type="submit" class="btn btn-primary">Załóż konto</button>
          </form>
          <% } %>
      </div>
    </div>
  </div>

<% include tiles/footerScript %>

<script>
$(function() {
  //alert("<= action %>");
});
</script>

<% include tiles/footer %>